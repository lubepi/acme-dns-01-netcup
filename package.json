{
  "name": "acme-dns-01-netcup",
  "version": "1.2.0",
  "description": "Netcup DNS for Let's Encrypt / ACME dns-01 challenges with ACME.js and Greenlock.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node test.js"
  },
  "keywords": [
    "acme",
    "acme-dns-01",
    "dns-01",
    "letsencrypt",
    "ssl",
    "tls",
    "certificate",
    "netcup",
    "greenlock",
    "greenlock-dns-01"
  ],
  "author": "lubepi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lubepi/acme-dns-01-netcup.git"
  },
  "bugs": {
    "url": "https://github.com/lubepi/acme-dns-01-netcup/issues"
  },
  "homepage": "https://github.com/lubepi/acme-dns-01-netcup#readme",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "acme-dns-01-test": "^3.3.2",
    "typescript": "^5.7.0"
  }
}
